import React from 'react'

export default class LinkedList {
  constructor(props) {
    super(props);
    this.state = { data:[] };
  }

  render() {
    return (
      <div className="">
        <h2>Linked List</h2>
        
      </div>
    )
  }
}